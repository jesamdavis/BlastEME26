{
  "name": "blasteme",
  "version": "0.1.0",
  "description": "BlastEME — a fast, provider-aware bulk sender. SEME is the brain/data-center; BlastEME only sends. Clicks, suppression, unsubscribe, and tracking all flow to SEME.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.3",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
